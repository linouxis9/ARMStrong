package projetarm_v2.simulator.core.io;

import projetarm_v2.simulator.core.RamRegister;

public class IOButton extends IOComponent {

	protected IOButton(RamRegister port, int shift) {
		super(port, shift);
	}
	
}
