package projetarm_v2.simulator.core.io;

import projetarm_v2.simulator.core.Register;

public class IOButton extends IOComponent {

	protected IOButton(Register port, int shift) {
		super(port, shift);
	}
	
}
